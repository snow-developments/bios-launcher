namespace Bios.Core;

/// <summary>
/// Normalized navigation intents the launcher understands, independent of the
/// physical device (keyboard, gamepad) that produced them.
/// </summary>
public enum InputCommand
{
    Up,
    Down,
    Select,
    Back,
}
